{{- if .Values.authManager.enabled }}
# ServiceAccount + Role granting auth-manager the cluster permissions it needs
# to manage per-tenant theme ConfigMaps and trigger a rolling restart of the
# platform-kc StatefulSet when a theme is uploaded.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Release.Name }}-auth-manager
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "auth-platform.labels" . | nindent 4 }}
    app.kubernetes.io/component: auth-manager
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ .Release.Name }}-auth-manager
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "auth-platform.labels" . | nindent 4 }}
    app.kubernetes.io/component: auth-manager
rules:
# Per-tenant theme ConfigMaps (created dynamically — cannot be name-scoped).
# Labeled mcpmesh/theme=true so the platform-kc theme-materializer init
# container can enumerate them.
- apiGroups: [""]
  resources: ["configmaps"]
  verbs: ["get", "list", "create", "update", "patch", "delete"]
# Patch the platform-kc StatefulSet's pod-template annotations to trigger a
# rolling restart (kubectl rollout restart equivalent). STS name varies per
# environment (Bitnami chart names it <release>-keycloak) — parameterized
# via .Values.authManager.kcStatefulSetName.
- apiGroups: ["apps"]
  resources: ["statefulsets"]
  resourceNames: [{{ .Values.authManager.kcStatefulSetName | default "platform-kc-keycloak" | quote }}]
  verbs: ["get", "patch", "update"]
- apiGroups: ["apps"]
  resources: ["statefulsets/status"]
  resourceNames: [{{ .Values.authManager.kcStatefulSetName | default "platform-kc-keycloak" | quote }}]
  verbs: ["get"]
# Patch deployments so CloudflaredReloader can trigger cloudflared rollouts
# after tunnel ingress changes. Same pattern as the StatefulSet patch above
# (kubectl.kubernetes.io/restartedAt annotation = strategic merge patch).
- apiGroups: ["apps"]
  resources: ["deployments"]
  verbs: ["get", "list", "watch", "patch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ .Release.Name }}-auth-manager
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "auth-platform.labels" . | nindent 4 }}
    app.kubernetes.io/component: auth-manager
subjects:
- kind: ServiceAccount
  name: {{ .Release.Name }}-auth-manager
  namespace: {{ .Release.Namespace }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ .Release.Name }}-auth-manager
{{- end }}
